/* Libraries ------------------------------------------------------- */
@import (reference) "@{path}/bootstrap/bootstrap.less";
@import (reference) "@{path}/local/utilities.less";
@import (reference) "@{path}/local/variables.less";
@path: "..";

/* Styles only for test page --------------------------------------- */
.sidebar__menu span {
  display: inline-block;
}
/* Responsive help line styles ------------------------------------- */
line {
  display: block;
  height: 2px;
  width: 100%;
  background-color: #f00;
  @media screen and (min-width: @screen-xs-min) {
    background-color: #0f0;
  }
  @media screen and (min-width: @screen-sm-min) {
    background-color: #00f;
  }
  @media screen and (min-width: @screen-md-min) {
    background-color: #ff0;
  }
  @media screen and (min-width: @screen-lg-min) {
    background-color: #f0f;
  }
}

/* Responsive help line styles ------------------------------------- */
.color-swatch {
  width: 100px;
  height: 100px;
  float: left;
  border-radius: 3px;
  margin: 5px;

  &.gray-darker {
    background-color: @gray-darker;
  }
  &.gray-dark {
    background-color: @gray-dark;
  }
  &.gray {
    background-color: @gray;
  }
  &.gray-light {
    background-color: @gray-light;
  }
  &.gray-lighter {
    background-color: @gray-lighter;
  }

  &.brand-primary {
    background-color: @brand-primary;
  }
  &.brand-success {
    background-color: @brand-success;
  }
  &.brand-info {
    background-color: @brand-info;
  }
  &.brand-warning {
    background-color: @brand-warning;
  }
  &.brand-danger {
    background-color: @brand-danger;
  }
}

/* Test page content block ----------------------------------------- */
.table-of-content {
  position: fixed;
  top: 20px;
  right: 0;
  z-index: 20;
  min-width: 240px;
  background: #fff;

  ul {
    list-style: none;
    padding-left: 0;
    a { padding: 3px 20px; }
    li {
      display: block;
      &.active {
        .box-shadow(inset 3px 0 0px -1px @brand-primary);
        & > a {
          font-weight: bold;
          font-size: 1.2em;
        }
      }
      & > ul {
        display: none;
        li {
          padding-left: 10px;
        }
      }
      &.active > ul {
        display: block;
      }
    }
  }
}




/* Font and sprite icons usage example ----------------------------- */
.icon {
  .make-xs-column(1);
  padding: 10px;
  background-color: @brand-primary;
  color: #fff;
  text-align: center;
  border: 1px solid #fff;
  min-height: 139px;
  .hover-state({
    background-color: #fafafa;
    color: @brand-primary;
  });
  .transition(all .1s);
  &-sprite {
    min-height: 110px;
    i {
      display: inline-block;
    }
  }
}

span {
  display: block;
}
header span {
  display: inline;
}
.name {
  font-size: 1.2rem;
  margin-top: 10px;
}
.glyphicon,
.fa {
  font-size: 2rem;
  .transition(all .1s);
}
.sprite {
  display: inline-block;
}
.glyphicon,
.fa {
  font-size: 3.5rem;
}
.sprite-full {
  border: 3px solid @brand-primary;
  background-color: #fafafa;
  .hover-active-state({
    background-color: @brand-primary;
  });
}

/* Fonts page samples ---------------------------------------------- */
.fontblock {
  width: 800px;
  margin: 0 auto 20px;
  overflow: hidden;
  display: block;

  h1 {
    margin-top: 300px;
  }
}
.samplebox {
  white-space: nowrap;
  margin: 20px 0 0;
}
.font-short-list {
  max-width: 1300px;
  margin: 0 auto;
  &:after {
    content: ' ';
    height: 0;
    display: block;
    overflow: hidden;
    clear: both;
  }
  p {
    margin: 0;
    padding: 0;
  }
  a {
    color: #000;
    text-decoration: none;
    display: inline-block;

    .hover-state ({
      color: @brand-primary;
    });
  }
  span {
    display: inline-block;
    overflow: hidden;
    width: 200px;

    &.samplebox {
      width: 400px;
      margin: 0 0 0 20px;
    }
  }
  .weight {
    text-align: right;
  }
  .title {
    font-weight: bold;
    color: @brand-primary;
  }
}
